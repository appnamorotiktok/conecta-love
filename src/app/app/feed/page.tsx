// PRÓXIMA ETAPA (Fase 1): cards de swipe (like/dislike), consultando `profiles`
// + `profile_photos`, gravando em `likes` (o match acontece automaticamente
// via trigger no banco quando os dois lados curtem).
export default function FeedPage() {
  return (
    <main className="flex min-h-screen items-center justify-center px-6 text-center">
      <div>
        <h1 className="text-xl font-semibold">Descobrir pessoas</h1>
        <p className="mt-2 text-muted-foreground">
          Em construção — próxima etapa do desenvolvimento.
        </p>
      </div>
    </main>
  );
}

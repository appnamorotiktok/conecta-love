// PRÓXIMA ETAPA (Fase 1): visualização/edição do próprio perfil, fotos,
// recomendações recebidas (seção "Pessoas que recomendam este perfil") e
// o link de "convide um amigo" (usa profiles.invite_token).
export default function ProfilePage() {
  return (
    <main className="flex min-h-screen items-center justify-center px-6 text-center">
      <div>
        <h1 className="text-xl font-semibold">Meu perfil</h1>
        <p className="mt-2 text-muted-foreground">
          Em construção — próxima etapa do desenvolvimento.
        </p>
      </div>
    </main>
  );
}
